'use strict';

var expect = require('chai').expect;

describe("", () => {
	it("", () => {
		expect().to.equal("");
	});
});

describe("", () => {
	it("", () => {
		expect().to.equal("");
	});
});

/* check if the number of files being watched by getWatched is the same as the number of files in the directory
*/

/* check if moving a file out of the directory causes the getWatched to equal one less than before
*/

/* check if moving the file back causes the getWatched equal the same number before being moved out
*/

/* check that moving a *.txt file into the directory will raise an exception
*/

/* check that a file that doesn't have a valid model raises an exception
*/

/* check that the log files are not empty
*/

